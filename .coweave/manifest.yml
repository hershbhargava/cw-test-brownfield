apiVersion: coweave.ai/v2
kind: TestEnvironment
metadata:
  name: widget-service-test-env
  generatedBy: qa-test-env-author-workflow
spec:
  stack: other
  entrypoint:
    compose:
      file: docker-compose.yml
  tests:
    command: "npm install && mkdir -p coverage && c8 --reporter=lcovonly --reporter=text --reports-dir=coverage npm test"
    timeoutSec: 600
