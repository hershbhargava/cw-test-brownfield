apiVersion: coweave.ai/v2
kind: TestEnvironment
metadata:
  name: widget-service-test-env
  generatedBy: qa-test-env-author-workflow
spec:
  stack: other
  entrypoint:
    compose:
      file: docker-compose.yml
  tests:
    command: "npm install && mkdir -p coverage && node --test --experimental-test-coverage --test-reporter=spec --test-reporter-destination=stdout --test-reporter=lcov --test-reporter-destination=coverage/lcov.info src/"
    timeoutSec: 1800
